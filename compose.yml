services:
  fastlane:
    build:
      dockerfile: Dockerfile
    image: fastlane
    ports:
      - "8080:8080"
      - "3000:3000"
    networks:
      - fastlane_network

networks:
  fastlane_network:
    external: true
